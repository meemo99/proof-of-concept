{% render 'partials/head.liquid' %}
<main>
  <nav arialabel="breadcrumb" class="breadcrumb">
    <a href="/">← Homepagina</a>
      <span>/</span>
      <span>{{ cityName }}</span>
      </nav>
  <h1> QuickScan {{ cityName }}</h1>
  

  <section class="card-container">
    <nav arialabel="tabs" class="tabs-container">
      <a href="#analytics" class="tab active">Analytics</a>
      <a href="#locaties" class="tab">Locaties</a>
    </nav>

    <section id="analytics" class='tab-content card'>
        <section class="monitoring total-data">
            <p><strong>11/38</strong></p>
            <br> <p>Geschikt voor Monitoring</p>
        </section>
        <section class="smart-zone total-data">
            <p><strong>11/38</strong> </p>
            <br><p>Geschikt voor Smart Zone</p>
        </section>
        
        <section class="chart">
        </section>
        <section class="chart">
        </section>
    </section>

    <section id="locaties" class="tab-content card">
        <section class="location-card">
          <picture class="city-img">
            {% if city.picture %}
            <img class="city-img" src="https://fdnd-agency.directus.app/assets/{{ city.picture }}?width=600&quality=80" alt="{{ city.address }}" loading="lazy">
            {% else %}
            <img class="city-img" src="/assets/download.png" loading="lazy">
            {% endif %}
          </picture>
          <a href="/dashboard/{{ city.city }}/{{ city.address }}">
            <p>{{ city.address }}</p>
          </a>
        </section>
      {% endfor %}
      <button class="add-button"><a href="/form">Voeg toe</a></button>
    </section>
  </section>
</main>
</body>
{% render 'partials/foot.liquid' %}