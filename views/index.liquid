{% render 'partials/head.liquid' %}

{% for city in cities %}
    <section>
    <a href="/dashboard/{{ city.city }}"><h2>{{ city.city }}</h2></a>
    <img
        src="https://fdnd-agency.directus.app/assets/{{ city.picture}}?width=600&quality=80"
     
        alt="{{ city.address }}">
    <h3>{{ city.address }}</h3>
    </section>
{% endfor %}

</body>
{% render 'partials/foot.liquid' %}
